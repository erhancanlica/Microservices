server:
  port: 8012

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/erhancnlc/micro-service-configuration
          username: erhancnlc
          password: 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
          clone-on-start: true
          default-label: main

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

management:
  endpoints:
    web:
      exposure:
        include: busrefresh